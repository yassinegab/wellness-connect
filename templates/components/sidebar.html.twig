<aside class="w-72 flex flex-col border-r border-border bg-sidebar">
  {# Logo / Header #}
  <div class="flex h-16 items-center justify-between border-b border-border px-6">
    <div class="flex items-center gap-3">
      <div class="flex h-9 w-9 items-center justify-center rounded-lg bg-primary">
        ❤️
      </div>
      <span class="text-lg font-semibold text-sidebar-foreground">
        Santé & Bien-être
      </span>
    </div>
  </div>
  {# Navigation #}
  <nav class="flex-1 space-y-1 p-4">
    {% set menuItems = [
      {'id': 'nutrition', 'label': 'Alimentation et nutrition', 'icon': '🥗', 'href': '#'},
      {'id': 'mental', 'label': 'Suivi du bien-être mental', 'icon': '🧠', 'href': '#'},
      {'id': 'activity', 'label': 'Activité physique et sport', 'icon': '🏃', 'href': '#'},
      {'id': 'social', 'label': 'Services sociaux et prévention', 'icon': '👥', 'href': '/services'},
      {'id': 'cycle', 'label': 'Suivi du cycle menstruel', 'icon': '🌸', 'href': '#'},
      {'id': 'settings', 'label': 'Paramètres', 'icon': '⚙️', 'href': '#'}
    ] %}
    {% for item in menuItems %}
      {% set isActive = (activeModule is defined and activeModule == item.id) %}
      <a href="{{ item.href }}" class="flex w-full items-center gap-3 rounded-lg px-4 py-3 text-sm font-medium transition-colors
        {{ isActive ? 'bg-sidebar-accent text-sidebar-primary' : 'text-sidebar-foreground/70 hover:bg-sidebar-accent hover:text-sidebar-foreground' }}">
        <span class="text-lg">{{ item.icon }}</span>
        {{ item.label }}
      </a>
    {% endfor %}
  </nav>
  {# Footer #}
  <div class="border-t border-border p-4">
    <div class="rounded-lg bg-muted p-4">
      <p class="text-xs text-muted-foreground">Dernière synchronisation</p>
      <p class="mt-1 text-sm font-medium text-sidebar-foreground">Aujourd'hui, 14:32</p>
    </div>
  </div>
</aside>