{% extends 'admin/base.html.twig' %}

{% block body %}
<div class="p-6 space-y-6">

<h1 class="text-2xl font-bold text-blue-700">
    🧾 Gestion des Dons
</h1>

<a href="{{ path('admin_don', {sort: sort == 'ASC' ? 'DESC' : 'ASC'}) }}"
   class="px-4 py-2 bg-gray-800 text-white rounded">
   Trier par date
</a>

<table class="w-full bg-white rounded shadow mt-4">
    <thead class="bg-gray-100">
        <tr>
            <th class="p-3">Type</th>
            <th>Région</th>
            <th>Date</th>
            <th>Statut</th>
            <th>Actions</th>
        </tr>
    </thead>
    <tbody>
    {% for don in dons %}
        <tr class="border-t">
            <td class="p-3">{{ don.typeDon }}</td>
            <td>{{ don.region }}</td>
            <td>{{ don.dateDon|date('d/m/Y') }}</td>
            <td>{{ don.statut }}</td>
            <td class="flex gap-2 p-3">
                <a href="{{ path('admin_don_validate', {id: don.id}) }}"
                   class="px-3 py-1 bg-green-600 text-white rounded">
                   Valider
                </a>

                <form method="post"
                      action="{{ path('admin_don_delete', {id: don.id}) }}">
                    <button class="px-3 py-1 bg-red-600 text-white rounded">
                        Supprimer
                    </button>
                </form>
            </td>
        </tr>
    {% endfor %}
    </tbody>
</table>

</div>
{% endblock %}
