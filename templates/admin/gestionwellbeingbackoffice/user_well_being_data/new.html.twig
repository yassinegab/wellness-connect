{% extends 'admin/base.html.twig' %}

{% block title %}New User Well-Being Data{% endblock %}

{% block body %}
<div class="space-y-6">

    <!-- Page Header -->
    <div class="flex items-center justify-between">
        <h1 class="text-2xl font-bold text-gray-900">Create New User Well-Being Data</h1>
        <a href="{{ path('app_user_well_being_data_index') }}" 
           class="inline-flex items-center gap-2 rounded-lg bg-gray-200 px-4 py-2 text-gray-800 text-sm font-medium hover:bg-gray-300 transition">
            <i class="fas fa-arrow-left"></i> Back to List
        </a>
    </div>

    <!-- Form Card -->
    <div class="bg-white shadow-sm rounded-lg p-6">
        {{ include('admin/gestionwellbeingbackoffice/user_well_being_data/_form.html.twig') }}
    </div>

</div>
{% endblock %}
