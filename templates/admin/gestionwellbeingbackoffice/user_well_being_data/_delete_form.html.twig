<form method="post" action="{{ path('app_user_well_being_data_delete', {'id': user_well_being_datum.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ user_well_being_datum.id) }}">
    <button class="btn">Delete</button>
</form>
