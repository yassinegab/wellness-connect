{% extends 'base.html.twig' %}

{% block title %}Admin AI Wellness Dashboard{% endblock %}

{% block body %}
<div class="min-h-screen bg-slate-900 text-slate-100 flex flex-col">
    {# Simple admin top bar #}
    <header class="bg-slate-800 border-b border-slate-700 px-8 py-4 flex items-center justify-between">
        <div class="flex items-center gap-4">
            <div class="w-10 h-10 rounded-xl bg-indigo-600 flex items-center justify-center">
                <i class="fas fa-shield-halved"></i>
            </div>
            <h1 class="text-xl font-bold tracking-tight text-white">Administration <span class="text-indigo-400">AI</span></h1>
        </div>
        <div class="flex items-center gap-4">
            <a href="{{ path('admin_dashboard') }}" class="text-sm font-bold text-slate-400 hover:text-white transition-colors">Tableau de Bord</a>
            <div class="w-8 h-8 rounded-full bg-slate-700 border border-slate-600"></div>
        </div>
    </header>

    <main class="flex-1 max-w-7xl mx-auto w-full p-8 lg:p-12">
        
        <div class="mb-12">
            <h2 class="text-3xl font-black text-white mb-2">Analyse de Risque Populationnelle</h2>
            <p class="text-slate-400 font-medium">Insights générés par AI basés sur l'ensemble des données utilisateurs</p>
        </div>

        <div class="grid grid-cols-1 lg:grid-cols-3 gap-8">
            
            {# Main AI Insight #}
            <div class="lg:col-span-2 space-y-8">
                <div class="bg-slate-800 rounded-3xl p-10 border border-slate-700 shadow-2xl relative overflow-hidden group">
                    <div class="relative z-10">
                        <div class="flex items-center gap-3 mb-8">
                            <div class="w-12 h-12 rounded-2xl bg-indigo-500/20 flex items-center justify-center border border-indigo-400/20">
                                <i class="fas fa-brain text-indigo-400"></i>
                            </div>
                            <h3 class="text-sm font-black uppercase tracking-widest text-indigo-400">Intelligence Artificielle</h3>
                        </div>
                        
                        <div class="prose prose-invert max-w-none">
                            <p class="text-2xl md:text-3xl font-medium leading-relaxed text-slate-100">
                                {{ analysis|nl2br }}
                            </p>
                        </div>
                    </div>
                    
                    {# Decor #}
                    <div class="absolute -right-20 -bottom-20 w-96 h-96 bg-indigo-500/10 rounded-full blur-3xl group-hover:bg-indigo-500/20 transition-colors duration-1000"></div>
                </div>
            </div>

            {# Quick Actions / Info #}
            <div class="space-y-6">
                <div class="bg-indigo-600 rounded-3xl p-8 text-white shadow-xl shadow-indigo-500/20">
                    <h4 class="font-bold text-lg mb-4">Sécurité & Éthique</h4>
                    <p class="text-sm text-indigo-100 leading-relaxed mb-6">
                        Toutes les données sont anonymisées pour l'analyse de groupe. L'IA se base sur des scores de tendance et non sur l'identité des utilisateurs.
                    </p>
                    <div class="flex items-center gap-2 text-xs font-bold bg-white/10 p-3 rounded-xl border border-white/10">
                        <i class="fas fa-lock"></i> Conformité RGPD Active
                    </div>
                </div>

                <div class="bg-slate-800 rounded-3xl p-8 border border-slate-700">
                    <h4 class="font-bold text-lg mb-4 text-white">Exportation Data</h4>
                    <p class="text-xs text-slate-400 mb-6">Générez un rapport PDF complet incluant ces insights AI pour la direction.</p>
                    <button class="w-full bg-slate-700 hover:bg-slate-600 py-3 rounded-xl font-bold transition-all disabled:opacity-50" disabled>
                        Générer Rapport PDF
                    </button>
                </div>
            </div>

        </div>

        {# Disclaimer #}
        <footer class="mt-20 pt-8 border-t border-slate-800/50 text-center">
            <p class="text-[10px] text-slate-500 uppercase tracking-widest font-black">
                AI Wellness Admin Support • Version 2.5
            </p>
        </footer>
    </main>
</div>
{% endblock %}
