{% extends 'layout/dashboard.html.twig' %}

{% block title %}Contacts d’urgence{% endblock %}

{% block content %}

<h1 class="text-2xl font-bold mb-6">📞 Contacts d’urgence</h1>

{# ADD CONTACT FORM #}
<div class="bg-white p-6 rounded-xl shadow-sm mb-6">
    <h2 class="font-semibold mb-3">➕ Ajouter un contact</h2>

   {{ form_start(form, { attr: { id: 'contactForm' } }) }}
    {{ form_widget(form) }}

    <p id="formError" class="hidden mt-3 text-sm text-red-600"></p>

    <button class="mt-3 px-4 py-2 bg-blue-600 text-white rounded">
        Enregistrer
    </button>
{{ form_end(form) }}

</div>

{# CONTACT LIST #}
<div class="grid gap-4">
    {% for contact in contacts %}
        <div class="bg-white p-4 rounded shadow flex justify-between items-center">
            <div>
                <strong>{{ contact.nom }}</strong><br>
                {{ contact.lien }} — {{ contact.telephone }}
            </div>

            <div class="flex gap-3">
                <a href="{{ path('contact_urgence_edit', {id: contact.id}) }}"
                   class="text-blue-600">
                   Modifier
                </a>

                <a href="{{ path('contact_urgence_delete', {id: contact.id}) }}"
                   class="text-red-600">
                   Supprimer
                </a>
            </div>
        </div>
    {% else %}
        <p class="text-gray-500">Aucun contact enregistré.</p>
    {% endfor %}
</div>
<script src="/js/contact-validation.js"></script>

{% endblock %}
