{% extends 'base.html.twig' %}

{% block title %}Détails Rendez-vous #{{ rendez_vous.id }}{% endblock %}

{% block body %}
<div style="max-width: 900px; margin: 0 auto; background: white; border-radius: 16px; padding: 40px; box-shadow: var(--shadow-md);">
    <h1 style="margin-bottom: 30px;">📋 Détails du Rendez-vous #{{ rendez_vous.id }}</h1>
    
    <div style="display: grid; gap: 20px;">
        <div style="padding: 16px; background: var(--bg-secondary); border-radius: 8px;">
            <strong>🏥 Hôpital:</strong> {{ rendez_vous.hopital.nom }}
        </div>
        <div style="padding: 16px; background: var(--bg-secondary); border-radius: 8px;">
            <strong>👤 Patient:</strong> {{ rendez_vous.patient.fullName }}
        </div>
        {% if rendez_vous.medecin %}
        <div style="padding: 16px; background: var(--bg-secondary); border-radius: 8px;">
            <strong>👨‍⚕️ Médecin:</strong> {{ rendez_vous.medecin.fullName }}
        </div>
        {% endif %}
        <div style="padding: 16px; background: var(--bg-secondary); border-radius: 8px;">
            <strong>📅 Date:</strong> {{ rendez_vous.dateRendezVous|date('d/m/Y à H:i') }}
        </div>
        <div style="padding: 16px; background: var(--bg-secondary); border-radius: 8px;">
            <strong>💻 Type:</strong> {{ rendez_vous.typeConsultation }}
        </div>
        <div style="padding: 16px; background: var(--bg-secondary); border-radius: 8px;">
            <strong>📊 Statut:</strong> {{ rendez_vous.statut }}
        </div>
        {% if rendez_vous.notes %}
        <div style="padding: 16px; background: var(--bg-secondary); border-radius: 8px;">
            <strong>📝 Notes:</strong><br>
            {{ rendez_vous.notes }}
        </div>
        {% endif %}
    </div>

    <div style="margin-top: 30px; display: flex; gap: 12px;">
        <a href="{{ path('app_rendez_vous') }}" class="btn btn-secondary">
            <i class="fas fa-arrow-left"></i> Retour à la liste
        </a>
        <a href="{{ path('app_rendez_vous_edit', {'id': rendez_vous.id}) }}" class="btn btn-primary">
            <i class="fas fa-edit"></i> Modifier
        </a>
    </div>
</div>
{% endblock %}