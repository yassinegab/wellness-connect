<form method="post" action="{{ path('app_analyse_a_i_delete', {'id': analyse_a_i.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ analyse_a_i.id) }}">
    <button class="btn">Delete</button>
</form>
