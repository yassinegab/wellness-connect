{% extends 'base.html.twig' %}

{% block title %}Analyze New Meal{% endblock %}

{% block body %}
<div class="min-h-screen bg-gray-50 flex flex-col">
    {# Top Navigation #}
    {% include 'components/top-nav.html.twig' %}

        <main class="flex-1 overflow-auto p-4 lg:p-6">
            <div class="max-w-3xl mx-auto">
                
                <div class="mb-6 flex items-center justify-between">
                    <h1 class="text-2xl font-bold text-gray-900">Analyze New Meal</h1>
                    <a href="{{ path('user_wellbeing_index') }}" class="text-gray-500 hover:text-gray-900 flex items-center gap-2">
                        <i class="fas fa-arrow-left"></i> Back
                    </a>
                </div>

                <div class="bg-white rounded-xl shadow-lg border border-gray-100 overflow-hidden">
                    <div class="bg-gradient-to-r from-emerald-600 to-teal-500 px-6 py-4">
                        <p class="text-emerald-50 text-sm font-medium">Power of AI</p>
                        <h2 class="text-white text-xl font-bold mt-1">Upload Your Meal Photo</h2>
                    </div>
                    
                    <div class="p-6 sm:p-8">
                         {{ form_start(form, {'attr': {'class': 'space-y-6', 'novalidate': 'novalidate', 'enctype': 'multipart/form-data'}}) }}

                            <!-- Image Upload Section -->
                            <div x-data="{ imagePreview: null }">
                                <label class="block text-sm font-medium text-gray-700 mb-2">Meal Photo</label>
                                
                                <div class="mt-1 flex justify-center px-6 pt-5 pb-6 border-2 border-gray-300 border-dashed rounded-xl hover:border-emerald-500 transition-colors bg-gray-50 group relative">
                                    
                                    {{ form_widget(form.imageName, {
                                        'attr': {
                                            'class': 'absolute inset-0 w-full h-full opacity-0 cursor-pointer z-10',
                                            '@change': 'imagePreview = URL.createObjectURL($event.target.files[0])'
                                        }
                                    }) }}

                                    <div class="space-y-1 text-center z-0 relative pointer-events-none">
                                        
                                        <div x-show="!imagePreview">
                                            <div class="mx-auto h-12 w-12 text-gray-400 group-hover:text-emerald-500 transition-colors">
                                                <i class="fa-solid fa-image text-3xl"></i>
                                            </div>
                                            <div class="flex text-sm text-gray-600 justify-center mt-2">
                                                <span class="relative rounded-md font-medium text-emerald-600 hover:text-emerald-500 focus-within:outline-none">
                                                    <span>Upload a file</span>
                                                </span>
                                                <p class="pl-1">or drag and drop</p>
                                            </div>
                                            <p class="text-xs text-gray-500 mt-1">PNG, JPG, GIF up to 5MB</p>
                                        </div>

                                        <div x-show="imagePreview" style="display: none;" class="relative">
                                            <img :src="imagePreview" class="mx-auto h-64 object-cover rounded-lg shadow-md" />
                                            <div class="absolute -top-2 -right-2 bg-red-500 text-white rounded-full p-1.5 shadow-sm z-20 pointer-events-auto cursor-pointer"
                                                 @click.stop="imagePreview = null; document.getElementById('{{ form.imageName.vars.id }}').value = ''">
                                                <i class="fa-solid fa-times text-xs"></i>
                                            </div>
                                        </div>

                                    </div>
                                </div>
                                 <p class="text-red-500 text-sm mt-1">{{ form_errors(form.imageName) }}</p>
                            </div>

                            <!-- Description -->
                            <div>
                                 {{ form_label(form.description, 'Description (Optional)', {'label_attr': {'class': 'block text-sm font-medium text-gray-700'}}) }}
                                 <div class="mt-1">
                                    {{ form_widget(form.description, {'attr': {'class': 'shadow-sm focus:ring-emerald-500 focus:border-emerald-500 block w-full sm:text-sm border-gray-300 rounded-md p-3', 'placeholder': 'E.g. Grilled salmon with asparagus...'}}) }}
                                 </div>
                                 <p class="text-gray-400 text-xs mt-1">Providing ingredients helps the AI be more accurate.</p>
                                 <p class="text-red-500 text-sm mt-1">{{ form_errors(form.description) }}</p>
                            </div>

                            <!-- Submit -->
                            <div class="pt-4 border-t border-gray-100 flex items-center justify-end gap-4">
                                <a href="{{ path('user_wellbeing_index') }}" class="text-gray-600 hover:text-gray-900 text-sm font-medium">Cancel</a>
                                <button type="submit" class="inline-flex justify-center py-2.5 px-6 border border-transparent shadow-sm text-sm font-medium rounded-md text-white bg-indigo-600 hover:bg-indigo-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-indigo-500 transition-all hover:scale-105">
                                    <i class="fa-solid fa-wand-magic-sparkles mr-2"></i> Analyze & Save
                                </button>
                            </div>

                        {{ form_end(form) }}
                    </div>
                </div>

            </div>
        </main>
</div>
{% endblock %}
